# 运行时、行动队列与存档 PRD

> 更新时间：2026-06-25
> 主要代码：`js/runtime.js`、`js/action-queue.js`、`js/world.js`、`js/main.js`、`js/systems/quest.js`、`serve.mjs`、`js/ui.js`。

## 1. 总目标

运行时系统负责把所有玩法系统接在同一个模拟循环上：时间推进、行动排队、移动、家具使用、互动执行、事件日志、存档和本地服务。

目标：

- 玩家和 AI 都通过同一行动队列执行行为。
- 行动失败能发出可诊断事件，不静默卡死。
- 人物寻路、社交站位和地图边界要保护，不能卡进空白地图。
- 游戏进度能本地保存、读取和清除。
- 本地服务支持静态资源、局域网访问和 Ollama 代理。

## 2. 当前实施状态

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| 主循环 | 已做 | `main.js` 小步推进模拟，并限制单帧模拟积压避免卡顿。 |
| AI 批处理 | 已做第一版 | 慢通道 AI 分批执行，减少页面瞬间卡顿。 |
| 游戏时间 | 已做 | `runtime.js` 推进游戏分钟、小时、天和时段事件。 |
| 行动队列 | 已做 | `enqueueAction` 支持玩家和 AI 入队。 |
| 移动 / 家具 / 互动 | 已做 | `action-queue.js` 统一处理移动、家具、互动和部分任务行为。 |
| 社交镜头 | 已修正 | 自动社交不再强行拉动镜头，点击走路不再因行动面板把镜头上推。 |
| 地图边界 | 已加固 | 社交不重叠和边缘处理需要继续回归，避免人物进空白地图。 |
| 存档面板 | 已做 | 前台底栏【存档】可保存、读取、清除当前浏览器本地进度，并给出反馈。 |
| 本地服务 | 已做 | `serve.mjs` 提供静态文件、Ollama 启动/代理和局域网地址输出。 |
| Docker | 已做 | `Dockerfile`、`docker-compose.yml` 可运行静态服务。 |

## 3. 行动队列口径

| 类型 | 来源 | 执行内容 |
| --- | --- | --- |
| `move` | 玩家点击、AI 候选、任务 | 寻路并移动到目标格。 |
| `furniture` | 家具候选、任务候选、起居需求块 | 使用家具动作，恢复需求或产生效果。 |
| `interaction` | 互动菜单、AI 社交、多人物反应 | 靠近目标并执行互动模板或 LLM 对话。 |
| `quest` / 派生项 | 任务系统 | 根据完成条件转成移动、家具、跟随、交互等行为。 |
| `follow` / duty | 随侍、职业日课 | 靠近目标或驻守场景，并允许需求危机打断后归队。 |

队列失败必须发 `queue:failed`，带上人物、候选 key、原因、目标场景或家具，供 AI 冷却和调试面板使用。

## 4. 存档范围

当前存档包含：

- 游戏时间、选中人物、队列页码、行动 ID。
- 人物位置、需求、状态、行动队列、记忆、技能、路径、钱。
- 关系数据。
- 家族、公账、经济、任务、任务下发、仆从关系、场景权限、人生路径、运势等系统状态。
- 起居配置、周历配置和人物当前覆盖作息。

配置不写入进度存档，而是单独存在 `dgy_config`。

## 5. 本地服务与 LLM

| 命令 | 用途 |
| --- | --- |
| `npm run start` | 启动静态服务，默认监听 `0.0.0.0:8765`。 |
| `npm run start:local` | 只监听 `127.0.0.1:8765`。 |
| `npm run tunnel` | 通过 cloudflared 暴露本地服务。 |
| `npm run test:release` | 发版前冒烟和配置检查。 |

`serve.mjs` 还提供 Ollama 相关能力：

- `/ollama` 代理模型请求。
- 可尝试启动 `ollama serve`。
- 文件模式打开游戏时，LLM 对话会提示改用本地服务。

## 6. 后续待办

- 存档版本迁移规则文档化，每次新增字段写迁移说明。
- 行动队列 item schema 统一化，减少各系统临时字段。
- 队列失败原因枚举化：不可达、权限不足、家具占用、目标离开、需求打断、任务阻塞。
- 服务脚本增加健康检查页，显示资源、Ollama 和当前端口状态。
