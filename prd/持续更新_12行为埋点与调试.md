# 行为埋点与调试 PRD

> 更新时间：2026-06-21
> 来源：当前代码、模拟报告和测试脚本。
> 主要代码：`js/systems/behavior-telemetry.js`、`js/ai.js`、`js/systems/quest.js`、`reports/`、`scripts/`。

## 1. 总目标

行为埋点系统用于回答“为什么 NPC 这么做”和“这套配置跑多天后是否合理”。

它服务三类问题：

- AI 决策解释：候选从哪里来、为什么被拒、为什么选中。
- 任务链排查：为什么没接、为什么阻塞、为什么完成质量低。
- 长期平衡：某人物几天内是否一直吃饭、一直社交、一直卡在某房。

## 2. 当前实施状态

| 模块 | 状态 | 说明 |
|---|---|---|
| 行为事件记录 | 已做 | `BehaviorTelemetry.append` 写入本地存储并可查询。 |
| AI 候选评估 | 已做 | 记录 provider、picked、topCandidates、scoreFactors、scoreHints。 |
| 候选拒绝 | 已做 | `ai:candidate_rejected` 进入 telemetry。 |
| 队列失败 | 已做 | `queue:failed` 和场景冷却可用于排查不可达。 |
| 任务埋点 | 已做 | 下发、接受、候选、阻塞、完成、失败等事件。 |
| 多日模拟报告 | 已做 | `scripts/simulate_xiaoxiang_7days.cjs` 生成报告。 |
| 自动测试 | 已做部分 | 经济、需求适应、需求状态有脚本。 |

## 3. 事件数据口径

通用事件应包含：

| 字段 | 含义 |
|---|---|
| `type` | 事件类型。 |
| `charId` | 主体人物。 |
| `ts` / `gameDay` / `gameHour` | 现实与游戏时间。 |
| `data` | 事件特定数据。 |

AI 决策事件建议包含：

| 字段 | 含义 |
|---|---|
| `channel` | fast、slow、urgent 等。 |
| `providerStats` | 各候选来源 checked、accepted、rejected。 |
| `picked` | 选中候选。 |
| `topCandidates` | Top 候选及权重。 |
| `scoreFactors` | 评分因子。 |
| `scoreHints` | 最关键解释文本。 |

## 4. 调试入口

- 浏览器本地存储：`dgy_behavior_telemetry_v1`。
- `BehaviorTelemetry.query(options)`：按人物、类型和时间查询。
- `BehaviorTelemetry.summarize(charId, options)`：人物行为摘要。
- `BehaviorTelemetry.download(charId)`：导出 JSON。
- `reports/`：保存模拟报告、布局 demo、检查点。

## 5. 验证命令

潇湘馆多日模拟的详细用法见：`reports/潇湘馆多日模拟使用手册.md`。

```bash
npm run test:economy
npm run test:needs
npm run test:need-states
node scripts/simulate_xiaoxiang_7days.cjs
```

## 6. 后续待办

- 做一个 AI 调试面板，展示当前意图、Top 候选、拒绝原因和最近失败冷却。
- telemetry 增加版本字段，避免配置变动后旧数据误读。
- 把任务时间线做成按人/日筛选的可视化回放。
- 增加“异常检测”：长时间无行动、候选全拒、频繁路径失败、需求长期危急。
